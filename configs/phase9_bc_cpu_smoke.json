{
  "paths": {
    "phase9_root": "outputs/phase9_d3rlpy",
    "output_root": "outputs/phase9_bc_cpu_smoke"
  },
  "dataset": "oulad",
  "training": {
    "n_steps": 10,
    "batch_size": 64,
    "learning_rate": 0.001,
    "gamma": 0.99,
    "accuracy_max_transitions": 256,
    "accuracy_batch_size": 256,
    "seed": 42
  },
  "notes": {
    "purpose": "Small CPU-only Behaviour Cloning development run.",
    "provisional_warning": "Use --allow-provisional only for smoke testing; provisional-state metrics are not final research results."
  }
}
